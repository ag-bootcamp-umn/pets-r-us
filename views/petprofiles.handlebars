<h1>Pets You've Matched {{{pets_number}}}</h1>
{{#each projects as |project| }}
<div class="row">
  <div class="col-6 col-md-4">
    <h2>
      {{{photo}}}
      {{!-- picture referance ie --}}
      {{!-- <a href="/project/{{project.id}}">{{project.name}}</a> --}}
      {{{name}}}
    </h2>
  </div>
</div>
{{/each}}
